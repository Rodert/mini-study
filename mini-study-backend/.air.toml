# Air configuration for hot reload
root = "."
cmd = "go run ./cmd/server"
bin = "./tmp/main"
delay = 1000

[build]
  include_ext = ["go", "tpl", "tmpl", "html", "yaml", "yml"]
  exclude_dir = ["vendor", "tmp", "storage/uploads"]
  args_bin = []
  log = "air.log"

[color]
  main = "cyan"
