app:
  env: prod
server:
  port: 8080
  allowed_origins:
    - https://mini-study.example.com
database:
  driver: mysql
  dsn: root:root@tcp(mysql:3306)/mini_study?charset=utf8mb4&parseTime=True&loc=Local
jwt:
  secret: prod-secret-change-me
swagger:
  enabled: false
